# Contributing

Start with a task brief and keep commits semantic and scoped. Update VERSION,
manifest, changelog, roadmap, and completion matrix when releasing. Add or update an
evaluation scenario for changed behavior. Run `scripts/evaluate.sh`. Never include
credentials, generated host state, or unrelated user files.
