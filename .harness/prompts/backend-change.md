# Backend change prompt

Read the stack profile, service boundaries, and contracts. Define inputs,
authorization, invariants, transactions, idempotency, errors, timeouts, logs, and
metrics. Test success, invalid input, unauthorized access, dependency failure, and
duplicate execution where applicable. Do not expose sensitive payloads in evidence.
